# Registro de Pendientes y Deuda Técnica

## Tareas Detectadas en Código
- [ ] `AUDIT_REPORT.md:10` - de forma plana sin manejo de errores transaccionales ante fallos de lectura/escritura JSON.  ---  ### SIN_ACCION en partes estables: El script de recuperaci¾n de LevelDB (`deep_recover_urls.py`) realiza operaciones de lectura de archivos locales en memoria de forma adecuada (`bytearray()`), por lo que no requiere modificaciones en su l¾gica principal de procesamiento de bytes.
- [ ] `AUDIT_REPORT.md:15` - `chat_with_fallback` se encuentra incompleto (cortado al final de la definici¾n del bucle `for model in candidates:`), omitiendo la l¾gica de ejecuci¾n, manejo de excepciones (`GroqError` / HTTP status) y el mecanismo de respaldo ordenado. - **Refactorizaci¾n concreta**: ```python     def chat_with_fallback(self, messages: List[Dict[str, str]], temperature: float = 0.2, max_tokens: int = 250) -> Dict[str, Any]:         """         Executes chat completion cascading down available models.         Stops immediately on Auth failure (401).         """         working = self._get_stored("working_models") or []         selected = self._get_stored("selected_model")                  candidates = []         if selected:             candidates.append(selected)         for w in working:             if w not in candidates:                 candidates.append(w)          if not candidates:             try:                 candidates = self.list_models()[:6]             except Exception as e:                 return {"error": str(e), "status": classify_groq_error(str(e))}          from groq import Groq, APIStatusError         client = Groq(api_key=self.api_key)          for model in candidates:             try:                 response = client.chat.completions.create(                     model=model,                     messages=messages,                     temperature=temperature,                     max_tokens=max_tokens                 )                 self._set_stored("selected_model", model)                 return {                     "answer": response.choices[0].message.content,                     "model": model,                     "success": True,                     "status": "Success"                 }             except APIStatusError as e:                 err_str = str(e)                 err_type = classify_groq_error(err_str)                 if e.status_code == 401 or err_type == "Auth":                     return {"error": err_str, "status": "Auth"}                 # Continuar con el siguiente modelo ante 404, 429, etc.                 continue             except Exception as e:                 err_str = str(e)                 if classify_groq_error(err_str) == "Auth":                     return {"error": err_str, "status": "Auth"}                 continue          return {"error": "Ning·n modelo disponible pudo responder la solicitud.", "status": "Exhausted"} ```
- [ ] `RULES.md:44` - proyecto debe adherirse estrictamente a SemVer (`MAJOR.MINOR.PATCH`). Cualquier cambio que rompa la compatibilidad de contratos o interfaces incrementarß obligatoriamente la versi¾n *Major*.

## Sugerencias de Mejora (Groq Analysis)
- [ ] Integrar linting automático con pre‑commit hooks.
- [ ] Añadir tests unitarios para cada regla violada.
- [ ] Implementar pipeline CI que genere AUDIT_REPORT.md automáticamente.
- [ ] Documentar ejemplos de refactorización en un wiki.
- [ ] Asegurar que todas las dependencias sigan SemVer y actualizar versiones en lockfiles.
